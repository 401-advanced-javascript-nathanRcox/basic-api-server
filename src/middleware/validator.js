'use strict';

// This is the validator from lab 2: use it to make a validator for lab 3. 

// const validate = (req, res, next) => {
//   const name = req.query.name;
//   if(!name) { next('A name is required'); }
//   else { next(); }
// }